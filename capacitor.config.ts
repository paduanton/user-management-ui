import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.ionic.demo.pg.cap.ng",
  appName: "User Management UI",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
